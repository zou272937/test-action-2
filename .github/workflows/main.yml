name: test-2

on:
  workflow_run:
    workflows: ["test-1"]
    types:
      - completed
  workflow_call:
    inputs:
      branch_name:
        required: false
        type: string
        default: main

jobs:
  test-job-2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: test-step-2
        run: echo "Project 2"
